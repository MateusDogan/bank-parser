# URL do backend. Em dev local, o padrao ja e http://localhost:8080 (nao
# precisa criar .env.local so pra isso). Sobrescrever quando o backend estiver
# em outro host (VPS, Lightsail, etc.) ou ao publicar em GitHub Pages.
VITE_API_URL=http://localhost:8080
