# DTOs

Objetos de transferência de dados, separados das entidades JPA (`entity/`) para que o contrato da API não quebre quando o schema do banco mudar.

Preenchido progressivamente nas Fases 1, 3 e 4 conforme os respectivos módulos são implementados.
